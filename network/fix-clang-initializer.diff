--- a/src/config/address.cpp
+++ b/src/config/address.cpp
@@ -59,7 +59,7 @@ address::address(const messages::address_item::cptr& message) NOEXCEPT
 }

 address::address(const asio::endpoint& uri) NOEXCEPT
-  : address(endpoint{ uri })
+  : address(address{ endpoint{ uri } })
 {
 }
