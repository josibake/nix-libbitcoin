diff --git a/compat/libbitcoin_bridge/include/ufsecp_libbitcoin.h b/compat/libbitcoin_bridge/include/ufsecp_libbitcoin.h
index c87a789..4a4f9ab 100644
--- a/compat/libbitcoin_bridge/include/ufsecp_libbitcoin.h
+++ b/compat/libbitcoin_bridge/include/ufsecp_libbitcoin.h
@@ -130,0 +131,7 @@ const char*       ufsecp_lbtc_ctrl_device_name(const ufsecp_lbtc_ctrl* ctrl);
+/* ------------------------------------------------------------------------- */
+/* Cancellation compatibility.                                                */
+/* ------------------------------------------------------------------------- */
+
+typedef int (*ufsecp_cancel_fn)(const void* user);
+typedef struct { ufsecp_cancel_fn callback; const void* user; size_t interval; } ufsecp_cancel_token;
+
@@ -192 +199,2 @@ ufsecp_error_t ufsecp_lbtc_verify_ecdsa(ufsecp_lbtc_ctrl* ctrl,
-                                        size_t* invalid_count);
+                                        size_t* invalid_count,
+                                        const ufsecp_cancel_token* cancel);
@@ -198 +206,2 @@ ufsecp_error_t ufsecp_lbtc_verify_ecdsa_opaque(ufsecp_lbtc_ctrl* ctrl,
-                                        size_t* invalid_count);
+                                        size_t* invalid_count,
+                                        const ufsecp_cancel_token* cancel);
@@ -204 +213,2 @@ ufsecp_error_t ufsecp_lbtc_verify_ecdsa_compact(ufsecp_lbtc_ctrl* ctrl,
-                                        size_t* invalid_count);
+                                        size_t* invalid_count,
+                                        const ufsecp_cancel_token* cancel);
@@ -210 +220,2 @@ ufsecp_error_t ufsecp_lbtc_verify_schnorr(ufsecp_lbtc_ctrl* ctrl,
-                                        size_t* invalid_count);
+                                        size_t* invalid_count,
+                                        const ufsecp_cancel_token* cancel);
@@ -232 +243,2 @@ void ufsecp_lbtc_verify_ecdsa_columns(ufsecp_lbtc_ctrl* ctrl,
-                                      uint8_t* results);
+                                      uint8_t* results,
+                                      const ufsecp_cancel_token* cancel);
@@ -239 +251,2 @@ void ufsecp_lbtc_verify_schnorr_columns(ufsecp_lbtc_ctrl* ctrl,
-                                        uint8_t* results);
+                                        uint8_t* results,
+                                        const ufsecp_cancel_token* cancel);
@@ -684 +695 @@ public:
-                                       nullptr, 0, nullptr);
+                                       nullptr, 0, nullptr, nullptr);
@@ -689 +700 @@ public:
-                                         nullptr, 0, nullptr);
+                                         nullptr, 0, nullptr, nullptr);
@@ -698 +710 @@ public:
-            ctrl_, msg_hashes32, pubkeys33, sigs64, count, results);
+            ctrl_, msg_hashes32, pubkeys33, sigs64, count, results, nullptr);
@@ -704 +716 @@ public:
-            ctrl_, msg_hashes32, pubkeys_x32, sigs64, count, results);
+            ctrl_, msg_hashes32, pubkeys_x32, sigs64, count, results, nullptr);
@@ -715 +726 @@ public:
-                                       nullptr, 0, nullptr);
+                                       nullptr, 0, nullptr, nullptr);
@@ -720 +731 @@ public:
-                                         nullptr, 0, nullptr);
+                                         nullptr, 0, nullptr, nullptr);
@@ -913 +924 @@ public:
-            sizeof(Row) - UFSECP_LBTC_ECDSA_RECORD, results, nullptr, 0, nullptr);
+            sizeof(Row) - UFSECP_LBTC_ECDSA_RECORD, results, nullptr, 0, nullptr, nullptr);
@@ -924 +935 @@ public:
-            sizeof(Row) - UFSECP_LBTC_SCHNORR_RECORD, results, nullptr, 0, nullptr);
+            sizeof(Row) - UFSECP_LBTC_SCHNORR_RECORD, results, nullptr, 0, nullptr, nullptr);
diff --git a/compat/libbitcoin_bridge/src/ufsecp_libbitcoin.cpp b/compat/libbitcoin_bridge/src/ufsecp_libbitcoin.cpp
index b87dda9..9d1d67b 100644
--- a/compat/libbitcoin_bridge/src/ufsecp_libbitcoin.cpp
+++ b/compat/libbitcoin_bridge/src/ufsecp_libbitcoin.cpp
@@ -829 +829,3 @@ ufsecp_error_t ufsecp_lbtc_verify_ecdsa_opaque(
-        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count) {
+        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count,
+        const ufsecp_cancel_token* cancel) {
+    (void)cancel;
@@ -837 +839,3 @@ ufsecp_error_t ufsecp_lbtc_verify_ecdsa_compact(
-        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count) {
+        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count,
+        const ufsecp_cancel_token* cancel) {
+    (void)cancel;
@@ -845 +849,2 @@ ufsecp_error_t ufsecp_lbtc_verify_ecdsa(
-        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count) {
+        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count,
+        const ufsecp_cancel_token* cancel) {
@@ -847 +852 @@ ufsecp_error_t ufsecp_lbtc_verify_ecdsa(
-                                           invalid_idx, invalid_cap, invalid_count);
+                                           invalid_idx, invalid_cap, invalid_count, cancel);
@@ -853 +858,3 @@ ufsecp_error_t ufsecp_lbtc_verify_schnorr(
-        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count) {
+        uint8_t* results, size_t* invalid_idx, size_t invalid_cap, size_t* invalid_count,
+        const ufsecp_cancel_token* cancel) {
+    (void)cancel;
@@ -878 +885,3 @@ void ufsecp_lbtc_verify_ecdsa_columns(ufsecp_lbtc_ctrl* ctrl,
-                                      uint8_t* results) {
+                                      uint8_t* results,
+                                      const ufsecp_cancel_token* cancel) {
+    (void)cancel;
@@ -888 +897,3 @@ void ufsecp_lbtc_verify_schnorr_columns(ufsecp_lbtc_ctrl* ctrl,
-                                        uint8_t* results) {
+                                        uint8_t* results,
+                                        const ufsecp_cancel_token* cancel) {
+    (void)cancel;
